{
  "name": "hass-config-generator",
  "version": "1.0.0",
  "description": "TypeScript-based Home Assistant configuration generator",
  "main": "dist/generate.js",
  "scripts": {
    "build": "tsc",
    "generate": "npm run build && node dist/generate.js",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "home-assistant",
    "configuration",
    "generator"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "yaml": "^2.3.4"
  }
}
