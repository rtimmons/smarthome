- name: set hostname
  hostname:
    name: "{{ secret.host.hostname }}"

- name: replace /etc/hosts
  template:
    src: etc_hosts.j2
    dest: /etc/hosts

- name: create new inventory file
  local_action:
    module: template
    src: inventory.j2
    dest: "{{ playbook_dir }}/post_init.ini"
