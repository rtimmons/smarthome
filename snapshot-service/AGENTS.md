# Agents — Snapshot Service

- Read the repo-wide guide: `../AGENTS.md`.
- Port: 4010 for local dev (`just dev` from root also starts it).
- Setup/dev: `just setup` then `just dev` here for the service only; `just test`, `just lint`, `just fmt`, and `just typecheck` use the local `npm` scripts.
- Container check: `just container-test` builds the Home Assistant add-on via `talos`.
