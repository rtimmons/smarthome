# Numeric owner of /srv/usenet on the cloud host.
PUID=1000
PGID=1000

# IANA timezone name used for logs and schedules.
TZ=Etc/UTC

# Host-side loopback ports. These are never bound to a public interface.
SABNZBD_PORT=8080
PROWLARR_PORT=9696
